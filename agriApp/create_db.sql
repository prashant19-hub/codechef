-- Run this in DBeaver connected to your PostgreSQL server (for example, to the default 'postgres' database)
-- This will create the database used by the Go app.

CREATE DATABASE agriapp;

-- Optional: after creating the DB, reconnect to agriapp and run the app.
-- The app itself will also create the tables automatically on startup.
